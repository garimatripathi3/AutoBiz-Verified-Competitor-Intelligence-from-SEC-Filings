# Minimal set to run the full ADK pipeline in OFFLINE MOCK mode (no API key).
# The offline MockLlm is a real google.adk BaseLlm, so ADK orchestration,
# Runner, and sessions all run. requests is required because MockLlm now
# genuinely calls the SEC EDGAR 10-K tool and web search (no fabricated
# fallback data) even in offline/mock mode.
google-adk>=2.0.0
pandas>=2.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
requests>=2.28.0
